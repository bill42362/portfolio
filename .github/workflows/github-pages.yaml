name: GitHub Page
on:
  push:
    branches:
      - "**"
    tags:
      - "v*"

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Inject slug/short variables
        uses: rlespinasse/github-slug-action@v3.x
      # - name: Prepare env variables
      #   shell: bash
      #   run: echo "##[set-output name=docker_tag;]$(echo ${GITHUB_REPOSITORY}:${GITHUB_REF#refs/heads/})"
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Docker QEMU
        uses: docker/setup-qemu-action@v1
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Cache install-production
        uses: docker/build-push-action@v2
        with:
          target: install-production
          tags: ${{ github.repository }}:${{ env.GITHUB_REF_SLUG }}.install-production
          cache-from: type=registry,ref=${{ github.repository }}:${{ env.GITHUB_REF_SLUG }}.install-production
          cache-to: type=inline
          push: true
      - name: Cache install-develop
        uses: docker/build-push-action@v2
        with:
          target: install-develop
          tags: ${{ github.repository }}:${{ env.GITHUB_REF_SLUG }}.install-develop
          cache-from: |
            type=registry,ref=${{ github.repository }}:${{ env.GITHUB_REF_SLUG }}.install-production
            type=registry,ref=${{ github.repository }}:${{ env.GITHUB_REF_SLUG }}.install-develop
          cache-to: type=inline
          push: true
      - name: Cache build-client
        uses: docker/build-push-action@v2
        with:
          target: build-client
          tags: ${{ github.repository }}:${{ env.GITHUB_REF_SLUG }}.build-client
          cache-from: |
            type=registry,ref=${{ github.repository }}:${{ env.GITHUB_REF_SLUG }}.install-production
            type=registry,ref=${{ github.repository }}:${{ env.GITHUB_REF_SLUG }}.install-develop
            type=registry,ref=${{ github.repository }}:${{ env.GITHUB_REF_SLUG }}.build-client
          cache-to: type=inline
          push: true
